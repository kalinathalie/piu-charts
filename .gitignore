.claude/
node_modules/
*.log

# tesseract.js language data downloaded at runtime
*.traineddata

# downloaded ingestion caches (regenerable via npm run ingest:*)
.cache/

# scratch drop folder for manual cover imports (npm run import:covers)
incoming/
